
import { type AIModel } from '../schema';

export async function getAIModels(): Promise<AIModel[]> {
  // This is a placeholder declaration! Real code should be implemented here.
  // The goal of this handler is fetching all available AI models from the database.
  // It should return only active models with their pricing and capabilities information.
  // This data should be synced with OpenRouter's available models.
  return Promise.resolve([]);
}
